const names: string[] = ['abul', 'Kabul', 'babul'];
const rollNumbers: number[] = [12, 34, 45, 67];

rollNumbers.map(roll => roll.toString);
names.map(name => name.indexOf);


const name2 = ["name", 23, true, null];
name2.push(null);
name2.push(34);




// Tuple--------------
const user: [number, string, boolean] = [1, 'ashik', true];
const user1: [string, string] = ["Ashik", "Aziz"];
user[0] = 5;